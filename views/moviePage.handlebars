<section id="video">
     <a href="/" class="back-button"> 
        <i class="far fa-arrow-alt-circle-left"></i> 
    </a> 
    <video autoplay> 
        <source src="/video/{{ moviesrc }}"> 
    </video> <!-- Reviews --> 

    <article class="commets">
        <div v-for="review in reviews">
            <review-stars 
                :value="review.comments_rating" 
                :disabled="true">
            </review-stars>
            <i class="post-date">${ review.comments_date }</i>
            <p>
                ${ review.comments_copy}
            </p>
        </div>
    </article>
    <article class="add-comment" :class="{floatRight : reviews.length > 0}  "> 
        <h4 class="reviw-heading">Add a Review</h4> 
        <span class="hidden movId">{{movie}}</span> 
        <review-stars 
                :value="numStars" 
                :disabled="false">
            </review-stars>
        <textarea name="review" id="review" placeholder="What did you think?" v-model="review" ></textarea> 
        <input type="submit" value="Submit Your Review" v-on:click="addReview"> 
    </article> 
</section>